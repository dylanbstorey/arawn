[package]
name = "arawn-script-sdk"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Pre-compiled SDK for agent-generated Rust scripts running in the Arawn WASM sandbox"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
regex = "1"
