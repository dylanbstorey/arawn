[package]
name = "runtime-transform"
version = "0.0.0"
edition = "2021"
description = "Transform WASM runtime — dot-path and template interpolation on context data"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
