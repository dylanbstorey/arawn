[package]
name = "runtime-file-read"
version = "0.1.0"
edition = "2021"
description = "File read WASM runtime — reads a file and returns its contents"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
tempfile = "3"
