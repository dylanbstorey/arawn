[package]
name = "runtime-passthrough"
version = "0.1.0"
edition = "2021"
description = "Passthrough WASM runtime — echoes context unchanged"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
